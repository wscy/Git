server:
  port: 8080
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  thymeleaf:
  cache: false

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: classpath:com.demo_boot/Entiry/*
  configuration:
      map-underscore-to-camel-case: true
#showSql
#logging:
#  level:
#    com:
#      example:
#        mapper : debug

